 <table class='table'>
   <%- @list.active_members.each do |member| %>
      <tr>
         <td><%= member.full_name %></td>
         <td class='remove'>
            <%if @list.user == current_user && @list.user != member %>
               <%= link_to image_tag('cancel.png'), list_remove_membership_path(@list.id, member_id: member.id), remote: true, confirm: "Are you sure?" %>
            <%end%>     
         </td>
       </tr>  
   <%end%>
</table>
